pipeline {
    agent any
    tools {
        maven 'maven'
    }
    stages {
        stage("clean up") {
            steps {
                deleteDir()
            }
        }
        stage("Clone repo") {
            steps {
                sh "git clone https://github.com/laffetnour/explspring.git"
            }
        }
        stage("Generate backend image") {
            steps {
                dir("explspring") {
                    sh "mvn clean install"
                    sh "docker build -t docexplspring ."
                }
            }
        }
        stage("Run docker compose") {
            steps {
                dir("explspring") {
                    sh "docker-compose up -d"
                }
            }
        }
    }
}
